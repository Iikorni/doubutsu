<h1>Edit Prize</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.prize_path(@conn, :update, @prize)) %>

<span><%= link "Back", to: Routes.prize_pool_path(@conn, :show, @prize.prize_pool) %></span>
